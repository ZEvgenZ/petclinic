version: "2"
services:
  db:
    image: mysql
    environment:
      - MYSQL_DATABASE=db
      - MYSQL_USER=myuser
      - MYSQL_PASSWORD=1234
    #ports: 3306:3306
  app:
    image: grebec/app:86
   # command: ping db
    ports:
      - "8080:8080"
    #volumes:
    #  - .:/code
